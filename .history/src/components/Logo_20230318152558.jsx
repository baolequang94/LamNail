import React from "react";
import "./logo.css";

const Logo = ({ className }) => (
	<h1 className={`logo w-full text-3xl font-bold text-[#00aff0] ${className}`}>
		Lam.
	</h1>
);

export default Logo;
